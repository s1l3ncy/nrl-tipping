/* Placeholder — replaced automatically on the first GitHub Actions run.
   cloud_fetch.py scrapes the live Zero Tackle player ratings and rewrites this
   file as  window.NRL_PLAYERS = { "player name": {pos, pct}, ... }.
   Until then the front-end falls back to a flat per-player injury cost. */
window.NRL_PLAYERS = {};
