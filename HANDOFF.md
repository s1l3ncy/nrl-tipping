# HANDOFF — start here

This is the orientation doc for anyone (a new AI chat, or Josh, or another
developer) picking this project up cold. Read this file top to bottom and you'll
understand what the project is, how it fits together, and how to safely make and
ship a change. Deeper detail lives in the `docs/` folder, linked throughout.

---

## 1. What this project is (in one paragraph)

It's a personal **NRL footy-tipping web app**, and since **2026-09-12** it has exactly
one job: **maximise the probability that Brigitte finishes 1st** in the family footytips
comp. Not accuracy, not a podium — first place. Every round it works out which tips give
her the best chance of winning the whole thing, which sometimes means deliberately
tipping an underdog her rivals are all on the wrong side of. It's a single
self-contained HTML page backed by a few small JavaScript data files. The data (ladder,
draw, odds, injuries, player ratings, the comp standings) is scraped and rebuilt
automatically by a GitHub Actions job every 4 hours, so it stays current **even when
Josh's Mac is off**. The site is hosted free on GitHub Pages.

- **Live site:** https://s1l3ncy.github.io/nrl-tipping/
- **GitHub repo:** https://github.com/s1l3ncy/nrl-tipping (public — required for free Actions + Pages)
- **Owner:** Josh (GitHub username `s1l3ncy`). He owns the repo, the account, the
  `ODDS_API_KEY` and every deploy. Roosters supporter.
- **User:** **Brigitte**, Josh's wife (footytips display name exactly `Brigitte`). She
  has no club loyalties — Josh, handing the app over: *"she really wants to win."* Josh
  himself is now just another rival in the model ("Special unit", 10 points back).
- **The one hard rule USED to be** "the Sydney Roosters are always tipped". That rule was
  removed on 2026-09-12 and is not coming back — it was measured at **−6.5 points of
  comp-win chance** in a single finals game. See `docs/MODEL.md` §5 and
  `docs/STRATEGY.md`.
- **Season context:** built during the **2026** NRL season; the 2026 finals (rounds
  28–31) are on.

---

## 2. The mental model (how it all connects)

```
   PUBLIC SOURCES                 GITHUB ACTIONS (runs every 4h, Mac off)      
   Zero Tackle (ladder,           ┌─────────────────────────────────────────────┐
   fixtures, injuries,            │ 1. cloud_fetch.py   scrape → *_dump.html/txt │
   player ratings)                │                     + rewrite nrl_players.js │
   nrl.com (draw meta)   ───────► │ 2. parse_nrl.py     dumps → nrl_data.js      │
   The Odds API (odds)            │ 3. learn_model.py   results → nrl_learned.js │
                                  │ 4. freeze_tips.mjs  → nrl_tiplog.js          │
                                  │ 5. validate_*.py    gate: must pass          │
                                  │ 6. cp html → index.html ; git commit + push  │
                                  └─────────────────────────────────────────────┘
                                                     │
                                                     ▼
   BROWSER (phone/desktop)   ◄──────────  GitHub Pages serves index.html
   nrl-tipping-guide.html loads nrl_data.js + nrl_learned.js + nrl_players.js +
   nrl_comp.js + nrl_tiplog.js, and computes each tip in-page by solving the comp:
   which set of tips maximises P(Brigitte finishes 1st). No team is ever forced.
```

The single most important thing to understand: **the website is `index.html`,
which is a copy of `nrl-tipping-guide.html` made by the workflow.** Editing the
guide file does nothing to the live site until a workflow run copies it over.
See `docs/DEPLOY_AND_OPS.md`.

---

## 3. File map

| File | What it is |
|------|-----------|
| `nrl-tipping-guide.html` | **The app.** Self-contained: HTML + CSS + all model JS in one file. This is the source of truth for the front-end; `index.html` is a generated copy. |
| `index.html` | Auto-generated copy of the guide (the workflow does `cp nrl-tipping-guide.html index.html`). This is what GitHub Pages serves. **Do not hand-edit.** |
| `nrl_data.js` | Weekly data feed: `window.NRL_DATA` — teams, ladder, fixtures, odds, news. Generated by `parse_nrl.py`. |
| `nrl_learned.js` | Learning-loop memory + fitted params + Elo + backtest: `window.NRL_LEARNED`. Generated by `learn_model.py`. |
| `nrl_players.js` | Player ratings map: `window.NRL_PLAYERS` = `{ "player name": {pos, pct} }`. Generated by `cloud_fetch.py` from Zero Tackle ratings. Powers position/rating-weighted injuries. |
| `sw.js` | Network-first service worker (hosted only). Keeps the iPhone home-screen app auto-updating (shell + data) and provides the offline cache. **Not** a generated file — upload once; it isn't overwritten by runs. |
| `apple-touch-icon.png` / `favicon-32.png` | The rooster app icon (home screen + browser tab). Source files, not generated. Editable master: `rooster-icon.svg` (local folder only, not in the repo). |
| `cloud_fetch.py` | Runs on GitHub only. Scrapes the live sources and writes the dump files + `nrl_players.js`. |
| `parse_nrl.py` | Pure parser (no network). Dumps → `nrl_data.js`. Also grows the results memory in `nrl_learned.js`. Has a `--merge` mode for fast daily refresh. |
| `learn_model.py` | Pure fitter (no network). Re-fits params + Elo from the results memory → `nrl_learned.js`. |
| `validate_data.py` / `validate_learned.py` | Gate-keepers. The workflow refuses to publish if either fails. |
| `nrl_comp.js` | The footytips comp snapshot: standings, each member's picks for the round, season affinity profiles, per-round margin/score history and the fitted rival behaviour model (`beh`). Generated by `cloud_fetch.py`. **This is what makes the tip deterministic** — the browser and the jsdom freeze read the same file. |
| `reference_finals_solver.py` | REFERENCE ONLY, frozen 2026-09-12. A standalone Python implementation of the finals maths, written the same day as the in-page solver so the two could be compared. Nothing imports it; the workflow never runs it. |
| `reference/crosscheck.mjs` | Dev tool: boots the real page in jsdom and compares its P(1st) for every possible tip vector against the Python reference. Run from the repo root after ANY solver change. |
| `test_ios_viewport.py` | Headless Playwright suite for the iOS standalone layout: the no-`viewport-fit=cover` guard (WebKit 301994, 2026-08-18), the `--sat`/`--sab` JS backstops in both geometries, env-wins, pinch hold, Android/browser-tab neutrality. Run after any layout/viewport change. |
| `.github/workflows/update-nrl.yml` | The automation: schedule, steps, commit + push. |
| `nrl_lineups.js` | The named 17 per club for the round, from the Tuesday team-lists article. Lets a player named in the side cancel his own injury-table entry. Generated by `cloud_fetch.py`. |
| `nrl_lineups.prev.js` | The previous run's copy of the above, snapshotted **before** the rewrite so the change feed can diff who was named/omitted. Generated. |
| `*_dump.html` / `*_dump.txt` | Intermediate scraped files (ladder/draw/injuries/**odds**/results). Regenerated each run. `odds_dump.txt` comes from The Odds API (nrl.com fallback), everything else from Zero Tackle. |
| `draw_meta.json` | Per-fixture venue / venue city / UTC kick-off from nrl.com. Fed to `parse_nrl.py` via `--draw-meta`. The only source of stadium + kick-off. Regenerated each run. |
| `last_run.json` | Heartbeat written every run — timestamps plus counters (`fixturesWithOdds`, `playersRated`, `clubsWithLineups`, `changeEntries`…). **Check this first** when the site looks stale: fresh timestamp = the job ran and it's a data problem, not the cron. |
| `DESIGN_SPEC.md` | The 2026-07-29 UI design brief. Historical — where it disagrees with the code, the code wins (its §4.4 still carries a bug that was fixed during implementation). |
| `README.md` | User-facing overview (plain English). Mostly current; its injury description predates the ratings-based version — `docs/MODEL.md` is authoritative. |
| `SPEC.md` | Original technical build spec / data contract. |
| `WEEKLY_UPDATE.md` | Runbook for the update job. |
| `MODEL_IMPROVEMENTS.md` | Earlier model-change notes. |
| `GITHUB_SETUP.md` | One-time hosting setup steps. |
| `sources.md` | Where each data feed comes from + expected formats. |
| `HANDOFF.md` (this file) + `docs/` | The in-depth handoff pack. |

---

## 4. The deep-dive docs (read as needed)

- **`docs/ARCHITECTURE.md`** — the whole system and data flow, in detail.
- **`docs/MODEL.md`** — exactly how a tip is calculated: Elo, form, home/away, injuries (position × rating), odds blend, the learning loop, and (§5) the exact finals solver that turns those probabilities into the tips that maximise P(1st). Includes a glossary.
- **`docs/STRATEGY.md`** — *(new 2026-09-12)* the comp rules verified from the API, the standings, how the solver reasons in plain English, the adaptive "enter tips game by game" advice, the margin-habit finding and the sensitivity table. **This is the one to hand Josh or Brigitte.**
- **`docs/DATA_PIPELINE.md`** — every script, every data/dump file, and every schema.
- **`docs/DEPLOY_AND_OPS.md`** — how hosting works and the **exact steps to make a change and ship it** (this is the one you'll use most).
- **`docs/FRONTEND.md`** — the HTML's structure, element IDs, settings, render pipeline, caching, PWA.
- **`docs/GOTCHAS.md`** — the landmines already hit and how to avoid them. Read before deploying.
- **`docs/CHANGELOG.md`** — dated record of major changes and the reasoning behind them.

---

## 5. How to make a change (the short version)

1. **Edit locally** in this folder (e.g. change model logic in `nrl-tipping-guide.html`, or a scraper in `cloud_fetch.py`).
2. **Test locally** where possible: open the HTML in a browser; run the Python scripts / validators. **Try the live scrape too** — this note used to say the editing sandbox has no network, but on 2026-07-29 a full live run of `cloud_fetch.py` completed from it. Test it rather than assuming; that assumption is why nobody noticed odds had never worked.
3. **Upload the changed file(s) to GitHub** (drag-drop in the repo's web UI, or drive it in the browser).
4. **Run the workflow**: repo → Actions → "Update NRL tips" → Run workflow. This rebuilds the data, copies the HTML to `index.html`, and publishes.
5. **Verify** via the raw file URLs (with a `?v=N` cache-buster) and the live site. See `docs/DEPLOY_AND_OPS.md`.

**Golden rules**
- **The objective is P(Brigitte finishes 1st). No team is ever force-tipped.**
  (`LOCK_MODE="off"`.) *Until 2026-09-12 this rule read "the Roosters are always tipped —
  never 'fix' that, it's the whole point". It is now the opposite: forcing a tip costs
  win probability, and the app exists to maximise win probability.*
- Never hand-edit `index.html`, `nrl_data.js`, `nrl_learned.js`, or `nrl_players.js` — they're regenerated and your edit will be overwritten.
- Time-sensitive fields (odds/news) are best-effort and may be `null`. That's normal, not a bug. (Weather was removed entirely on 2026-08-04.)
- Finals are rounds 28–31 — numeric everywhere, `roundName` for display. Read rounds via `parse_nrl.round_from_text()`, never a fresh regex.
- Keep the HTML dependency-free (no CDNs) — it must work offline, opened as a local file.

---

## 6. Current state (as of the last update in this pack)

*(Reviewed 2026-09-12 — Finals Week 1, the Brigitte rebuild.)*

- **2026-09-12: the app is Brigitte's, and it solves the finals exactly.** New
  objective — **pure P(1st)**, no top-3/top-4 terms. `COMP_ME`/`FOOTYTIPS_ME` =
  `Brigitte`; identity is derived from that constant **by name** so a stale
  `nrl_comp.js` can't make the freeze and the browser play for different people.
  **The Roosters lock is gone end-to-end** (`LOCK_MODE="off"`), and with it the
  `g.lock` sim branches, the "never the Roosters game" filter, the loyalty ledger line,
  the 🔒/🐓/gold cosmetics, `#rkTax` and `learn_model.py`'s `lockTax`. In rounds 28–31
  `finalsPlan()` replaces the Monte-Carlo with **full enumeration + backward induction**
  over the real bracket (~330 ms, no PRNG): per-member fitted rival model (`beh`) plus a
  latent strategic layer, the margin **countback** priced properly (it is her whole
  edge), and the +2 perfect-round bonus alive-aware. First paint no longer waits for it —
  `compPlan()` returns a provisional plan from a stamped localStorage cache or the frozen
  tiplog and solves on an idle callback; **the freeze never defers**. An independent
  adversarial audit the same day fixed six things, the worst of which printed "chance of
  winning the comp: **100%**" while she was mathematically eliminated. R28 tips:
  **Dolphins / Sharks / Panthers, P(1st) ≈ 45%**. `sw.js` v23. Read `docs/STRATEGY.md`,
  then MODEL.md §5 and the 2026-09-12 GOTCHAS entries before touching any of it.

- **2026-09-07: the finals work.** Rounds 28–31 are Finals Week 1/2/3 + Grand
  Final in every source, but named differently in each; all naming now resolves
  through `parse_nrl.round_from_text()` and the pipeline keeps a single numeric
  round (`nrl_data.js` adds `roundName`/`finals`; `byeTeams` is `[]`). Club-count
  gates (injuries, team lists) and the draw gate scale with `FINALS_GAMES`; the
  validator accepts 1–4 fixtures + no byes. Front-end shows "Finals Week N", and
  reads "back Finals" as a doubt (suspension → available) once the finals are on.
  Verified with a live scrape + freeze (4/4 tips) + audit. *(That verification line
  originally read "SYD locked v PEN" — the lock is gone as of 2026-09-12.)*
  `sw.js` v22. GOTCHAS "Finals are rounds 28–31" before touching any round parsing.

- **2026-08-21 (later): the feed shows one flip per game.** `chgList()` keeps
  only the newest flip per game (unordered-pair dedupe; history stays in
  `nrl_tiplog.js`) and the feed's group sorts break same-severity ties
  newest-first (`chgTs`) — the two R25 SOU–NZW flips had rendered together,
  older on top. Front-end only; `sw.js` v21.

- **2026-08-21: the freeze loads the prior tiplog.** `freeze_tips.mjs` now inlines
  the committed `nrl_tiplog.js` into its jsdom page — the simulator reads the tiplog
  (perfect-round-alive via `gradedTip()`, plus the incumbency tie-break), so the old
  "strip it for independence" behaviour made the freeze compute different tips than
  every browser once a round had its first result (R25 SOU–NZW: What's new announced
  "now Warriors" while the Tips page correctly showed the Rabbitohs split). Script
  fix only; GOTCHAS "The freeze must LOAD the prior tiplog".

- **2026-08-15: the simulator prices the +2 perfect-round bonus in the current
  round** (you + rivals, alive-aware of games already played), fixes your
  unlocked-Roosters pick (you always tip the lock), adds **top 4** to the
  objective (`P(top4)+P(top3)+P(1st)`), and shows an honest baseline — "tip
  favourites all season" (~0.3% top 4) vs following the machine (~1.5%). A
  single round's split is ~neutral; the season-long chase is the edge. `sw.js`
  v19. GOTCHAS "perfect-round bonus" before touching `simComp()`.
  **PARTLY SUPERSEDED 2026-09-12:** the objective is pure P(1st) (no top-4/top-3
  terms) and there is no lock, so the unlocked-Roosters fix is moot. The bonus
  pricing and the favourites baseline survive — computed exactly in the finals.

- **2026-08-13 (night): the split policy is simulator-priced.** *(PARTLY SUPERSEDED
  2026-09-12: `simComp()` is the FALLBACK now — finals rounds are solved exactly by
  `finalsPlan()`, and the "balanced" objective is gone. The countback, the margin-game
  advice and the adherence tally all survive.)* An in-page,
  deterministic Monte-Carlo season simulator (`simComp()`) replaces the need
  bands for split selection under Josh's balanced objective (1st worth ~2× a
  podium): 3,000 sims of the rest of the season, rival herd rates fitted to
  season accuracy, future-me playing the same machine policy, incumbency +
  policy-consistency tie-breaks so MC noise can't churn tips. The comp panel
  shows honest P(1st)/P(top 3), a margin countback column + median-play
  margin-game advice (footytips breaks score ties on LOWER cumulative
  margin), and a "You vs the machine" adherence tally. Bands remain as
  fallback. MODEL.md (top) + GOTCHAS ("comp simulator") are required reading
  before touching `tipSide()`/`compPlan()`/`simComp()`. `sw.js` v17.

- **2026-08-13: comp picks as plain-text side groups + double-tap zoom fixed +
  live-poll hole closed.** The locked-card comp display is plain text grouped
  per picked team (crest dot + code + first names, ✓/✗ per side at FT — no
  pills, two UI iterations in one day); the Predicted strip matches.
  `touch-action:manipulation` in the universal `*` rule kills iOS double-tap
  zoom (pinch preserved — keep it universal, see GOTCHAS). And the live-score
  poll now survives nrl.com blanking a solo game's kickoff mid-match (±36h
  game-day window + today's date in the ESPN query — see GOTCHAS "Live
  scores"). Front-end + `sw.js` (v16) only; freeze confirmed zero tip changes.

- **2026-08-10 (night): the tip itself now optimises winning the comp.** *(PARTLY
  SUPERSEDED 2026-09-12 — the lock step is deleted and the need bands are only a
  candidate filter/fallback; in the finals the split set comes from the exact solver.
  The `predict()`/`tipSide()` seam, oddsW 0.75 and `.mkt` logging all stand.)*
  `tipSide()` = Roosters lock → need-banded underdog-split policy (fed by
  `nrl_comp.js`: standings, picks, rival profiles — pipeline-computed so the
  freeze and browser agree byte-for-byte) → blended favourite. oddsW 0.75
  market-heavy prior; market probs now logged per frozen tip. Read MODEL.md
  "THE OBJECTIVE CHANGED" before touching anything.

- **2026-08-10 (later): comp strategy mode, always on** — rival
  pick-prediction (84% backtested), "Predicted" strips on pre-lock cards, and
  ≤2 🎯 split-pick suggestions when trailing the comp. See FRONTEND + GOTCHAS.
  *(PARTLY SUPERSEDED 2026-09-12: "never the Roosters game" is gone, and in the finals
  rivals are predicted by the fitted per-member `beh` logistic plus a strategic layer
  rather than loyalty alone. Strategy is still always on — for Brigitte.)*

- **2026-08-09: the family footytips comp renders on Tips** (Josh's comp; Brigitte's
  since 2026-09-12) — member picks as
  chips on every locked game card (✓/✗ at full time) and a mini comp ladder
  under the Quick list. Fetched by the OPEN PAGE from footytips' public comp
  API (verified: no auth needed) — no pipeline involvement. Pre-lock picks are
  gated by `compLocked()` (a round's tips are sealed server-side until the
  round starts — no earlier view exists); display names only. `sw.js` v12. See FRONTEND "Friends' comp tips" + GOTCHAS before touching.

- **2026-08-08 (evening):** the pinned Roosters hero is GONE (card sits in
  bucket order; the lock itself survived until 2026-09-12); **tip flips lead the What's-new feed** with win %
  and a plain-English why (recorded by `freeze_tips.mjs` → `NRL_TIPLOG.flips`;
  ko-less fixtures never re-freeze mid-game); quick list + copyTips run in week
  order; the ↻ chip is gone (pull-to-refresh + auto-refresh remain). `sw.js` v9.

- **2026-08-08 (later): status-ordered Tips screen.** Live games first, then
  upcoming by kickoff (top card shows "· in N hours"), then played most-recent-
  first, with conditional "On now / Up next / Played" dividers; quick list and
  copyTips share the order. Reordering only happens in full renders (foreground
  return, or immediately within 15s of boot) — never on a 45s score tick.

- **2026-08-08: live in-play scores.** While a game is on, the open page polls
  ESPN's public NRL scoreboard every 45s (the only score source that sends CORS
  headers — nrl.com's JSON doesn't) and the game's card, the lock hero, the quick
  list and the schedule all show the live score + clock; full time appears
  immediately and the pipeline's official result takes over when it lands. The
  overlay is display-only (grading/results/tiplog untouched) and re-renders
  surgically so open folds survive. `sw.js` is at CACHE v7. Details:
  `docs/FRONTEND.md` "Live scores" + `docs/GOTCHAS.md` "Live scores (2026-08-08)".

- **2026-08-04 batch (see `docs/CHANGELOG.md` for the full entry):** the app now has a
  real desktop layout at ≥1024px (top pill nav, 2-up cards, rails) while the phone UI
  is unchanged; it keeps itself fresh while open (foreground-return refresh, 5-minute
  polling, a ↻ chip, pull-to-refresh) — no more force-quitting the home-screen app;
  the What's new screen was redesigned (Today + folded Earlier + a full round schedule
  panel); **weather was removed end-to-end**; and a specialist audit's nine bug fixes
  landed (opening odds preserved across runs, winner-relative Elo MOV, `logisticScale`
  pinned at 7, walk-forward loyalty tax, phantom-injury-name guards, season-aware
  results, and more — `docs/MODEL.md` + `docs/GOTCHAS.md`). `sw.js` was CACHE v6 in that batch.
  The audit's judgement-call recommendations await Josh's approval (CHANGELOG).

- Fully deployed and self-updating **every 4 hours** (plus 05:47 daily and 16:23 Tuesday
  for team lists).
- **Bookmaker odds are LIVE via The Odds API** — the `ODDS_API_KEY` repo secret is set,
  and run #33 published 7/7 open markets (median of 11 AU books). nrl.com geo-blocks its
  own prices outside Australia, which is why it's only the fallback. Watch
  `oddsApiRemaining` in `last_run.json` (free tier: 500/month, schedule uses ~240).
- **The front-end is a four-screen tab-bar app** (2026-07-30 rebuild): Tips / What's new
  (with unread badge) / Ladder / Model, glass-on-navy Roosters visual system, rebuilt
  game cards with a win-probability bar (gold meant "the locked Roosters game" then; as
  of 2026-09-12 gold marks a **comp split**, the 🎯 pick). The render
  pipeline and every element ID are untouched — see `docs/FRONTEND.md` before styling
  anything. There's a real app icon now (`apple-touch-icon.png`; source in
  `rooster-icon.svg`, kept local-only); `sw.js` has since moved to CACHE v7.
- **The round's team list settles doubts both ways**: named → injury entry cancelled;
  not named while merely doubtful → full-weight absence, badged NOT NAMED.
- ~~**The Roosters lock is genuinely enforced** via `tipSide()`~~ — **removed
  2026-09-12**, see the top of this section. The change feed
  (rolling 36h, folded past 8 rows, Sydney-labelled clock) and the plain-English
  "why" summary with the folded ledger are live.
- The learning loop is **past** the low-confidence guardrail (156+ logged games,
  `lowConfidence: false`) — the **Elo engine is what's actually running**, and `hga` /
  `oddsW` in Advanced settings default to the *learned* values. Check `nrl_learned.js` →
  `gamesLearned` / `lowConfidence` rather than trusting prose.

If anything here looks stale, trust the code over the prose, and update this pack.
